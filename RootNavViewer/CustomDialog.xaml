<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="RootNav.Viewer.CustomDialog"
        Title="CustomDialog" ResizeMode="NoResize" d:DesignWidth="420" d:DesignHeight="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="42" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Name="Yes" Width="80" Margin="3" />
            <Button Name="No" Width="80" Margin="3" />
        </StackPanel>
    </Grid>
</Window>
